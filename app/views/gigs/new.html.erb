<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Home" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Gigboard: New Gig" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <%= render :partial => 'navbar', locals: { post: @post } %>
    </header>
    <main role="main">
      <%= content_for?(:content) ? yield(:content) : yield %>
      <h1>New Gig Listing</h1>
      <%= form_with scope: :gig, local: true do |form| %>
        <p>
          <%= form.label :title %><br>
          <%= form.text_field :title %>
        </p>
        <p>
          <%= form.label :location %><br>
          <%= form.text_field :location %>
        </p>
        <p>
          <%= form.label :date %><br>
          <%= form.text_field :date %>
        </p>
        <p>
          <%= form.label :start_time %><br>
          <%= form.text_field :start_time %>
        </p>
        <p>
          <%= form.label :end_time %><br>
          <%= form.text_field :end_time %>
        </p>
        <p>
          <%= form.label :ask_price %><br>
          <%= form.text_field :ask_price %>
        </p>
        <p>
          <%= form.label :description %><br>
          <%= form.text_area :description %>
        </p>
        <p>
          <%= form.submit %>
        </p>
      <% end %>
    </main>
  </body>
</html>
