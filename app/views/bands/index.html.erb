<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Home" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Gigboard: Home" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <%= render :partial => 'navbar', locals: { post: @post } %>
    </header>
    <main role="main">
      <%= content_for?(:content) ? yield(:content) : yield %>
      <h1>Welcome to GigBoard!</h1>
      <h2>Bands</h2>
      <ul>
        <% @bands.each do |band| %>
          <li><%= band.name %> | Location: <%= band.location %> | <%= band.style %> <%= link_to 'Show', band %></li>
        <% end %>
      </ul>
      <%= link_to 'Add a new gig band', new_band_path %>
    </main>
  </body>
</html>
